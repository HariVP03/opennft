import { chakra, Flex } from "@chakra-ui/react";
import { Layout } from "@components/main";
import Head from "next/head";

const Saved = () => {
    return (
        <>
            <Head>
                <title>OpenNFT | Saved</title>
            </Head>
            <chakra.main>
                <Layout>
                    <Flex px={9}>sex</Flex>
                </Layout>
            </chakra.main>
        </>
    );
};

export default Saved;
